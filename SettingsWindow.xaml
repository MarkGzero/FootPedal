<Window x:Class="FootPedalApp.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Foot Pedal Settings" 
        SizeToContent="WidthAndHeight"
        MinWidth="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E1E">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFFFFF"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="CaretBrush" Value="#FFFFFF"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#FFFFFF"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#0E639C"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1177BB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Button Grid.Row="0" Grid.Column="2" 
                x:Name="btnConfig" Content="Config" 
                Height="30" 
                FontSize="12"
                HorizontalAlignment="Right"
                Padding="8,5"
                Margin="5,0,0,15" Click="ConfigButton_Click"
                ToolTip="Edit config.json"/>
        
        <Button Grid.Row="0" Grid.Column="3" 
                x:Name="btnHelp" Content="?" 
                Width="30" Height="30" 
                FontSize="16" FontWeight="Bold"
                HorizontalAlignment="Right"
                Margin="5,0,0,15" Click="HelpButton_Click"
                ToolTip="View syntax help"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Left Pedal:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" x:Name="txtLeft" Margin="0,0,0,10" Padding="5" MouseDoubleClick="TextBox_MouseDoubleClick" ToolTip="Double-click to edit in larger window"/>
        <Button Grid.Row="1" Grid.Column="3" Content="Capture" Width="70" Margin="5,0,0,10" Click="CaptureLeftButton_Click" ToolTip="Capture hotkey"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Middle Pedal:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" x:Name="txtMiddle" Margin="0,0,0,10" Padding="5" MouseDoubleClick="TextBox_MouseDoubleClick" ToolTip="Double-click to edit in larger window"/>
        <Button Grid.Row="2" Grid.Column="3" Content="Capture" Width="70" Margin="5,0,0,10" Click="CaptureMiddleButton_Click" ToolTip="Capture hotkey"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Right Pedal:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" x:Name="txtRight" Margin="0,0,0,10" Padding="5" MouseDoubleClick="TextBox_MouseDoubleClick" ToolTip="Double-click to edit in larger window"/>
        <Button Grid.Row="3" Grid.Column="3" Content="Capture" Width="70" Margin="5,0,0,10" Click="CaptureRightButton_Click" ToolTip="Capture hotkey"/>

        <CheckBox Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="4" x:Name="chkEnableTop" Content="Enable Top Pedal" Margin="0,0,0,5" Checked="TopPedalCheckBox_Changed" Unchecked="TopPedalCheckBox_Changed"/>
        
        <TextBlock Grid.Row="5" Grid.Column="0" Text="Top Pedal:" VerticalAlignment="Center" Margin="0,0,10,10" x:Name="lblTop"/>
        <TextBox Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2" x:Name="txtTop" Margin="0,0,0,10" Padding="5" MouseDoubleClick="TextBox_MouseDoubleClick" ToolTip="Double-click to edit in larger window"/>
        <Button Grid.Row="5" Grid.Column="3" x:Name="btnCaptureTop" Content="Capture" Width="70" Margin="5,0,0,10" Click="CaptureTopButton_Click" ToolTip="Capture hotkey"/>

        <TextBlock Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="4" 
                   TextWrapping="Wrap"
                   FontStyle="Italic" 
                   Foreground="#808080"
                   Margin="0,10,0,10">
            <Run Text="Examples:"/><LineBreak/>
            <Run Text="  WIN+1, CTRL+Z, SHIFT+A, ALT+F4"/><LineBreak/>
            <Run Text="  RUN:notepad.exe"/><LineBreak/>
            <Run Text="  RUN:notepad.exe C:\file.txt"/><LineBreak/>
            <Run Text="  URL:https://google.com"/><LineBreak/>
            <Run Text="  URL:chrome https://google.com"/>
        </TextBlock>

        <StackPanel Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button x:Name="btnSave" Content="Save" Width="80" Margin="0,0,10,0" Click="SaveButton_Click"/>
            <Button x:Name="btnCancel" Content="Cancel" Width="80" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
